# Evidence

Version 2 evidence must be produced by executing the scenarios against the
hosted disposable lab project. Successful output is never fabricated.

See [PENDING.md](PENDING.md) for the required captures.

Preferred formats:

- `.txt` for SQL plans, request/response headers and diagnostic queries
- `.json` for sanitized structured errors
- `.png` only when dashboard context materially proves log correlation

Screenshots must exclude passwords, complete JWTs, database credentials,
private conversation URLs, unrelated browser tabs and desktop notifications.
